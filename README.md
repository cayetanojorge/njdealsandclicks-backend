## 🛠️ Configurazione ambiente

Questa applicazione utilizza [Spring Boot](https://spring.io/projects/spring-boot) e supporta configurazioni multiple tramite i profili:

- `develop` (default)
- `test`
- `prod`

### 🔧 1. Variabili d'ambiente con `.env`

Copia il file `.env.example` e rinominalo in `.env`:

```bash
cp .env.example .env
